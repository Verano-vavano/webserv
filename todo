-Obligatoire
SUJET :
"poll() (ou équivalent) doit vérifier la lecture et l’écriture en même temps." ?
timeout serveur
Max body size
Disable POST according to methods
DELETE

BUGS:
Valgrind bugs

NOUS:
READ.ME
Body to
Body Buf size
Body infile
Header buf size
Ignore invalid headers
Keepalive (time)
Log not found
Log sub
Server_name
Allow /.. in directory listing ? (idk, seems dangerous)

-Potentiel
retrocompatibilite HTTP1.0
REGEX in nginx (crazy crazy, could be implemented in our shell lmao) (location ~ \.(gif|jpg|png)$)
max_depth ?
Equivalence lists pour Accept ? (genre html = text/html = text/htm = plain/text) et adapter en ft de q=
Opti location pour support wildcards et qu'un tour de boucle plutot que plusieurs find
Gérer requêtes POST en CGI (comme Nginx)
