#DEFINE SILENT;

http {
    server 8080bonjour{
        location / {
            root /data/www;
        }
        location /images {
            root /data;
        }
	location { root /data;;;;;}
    }

    server {
        listen 8080;
        location / {
            root /data/www;
        }
    }
}
