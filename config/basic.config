DEFINE ERROR_STOP;

http {
    server {
        location / {
            root /data/www;
        }
        location /images {
            root /data bonjour;
        }
    }

    server {
        listen 8080;
        location / {
            root /data/www;
        }
    }
}
